<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="./css/styles_home.css">
    <link rel="stylesheet" href="./css/styles_Sportal.css">

</head>
<body>
    <!-- search Teacher -->
    <header>
        <h1>STUDENT TEACHER APPOINTMENT PORTAL</h1>
        <p>Status of your appointment requests till date<br/>Approval of some requests may take some time</p>
     </header>

     
    <div class="book_outer">
                <div class ="navigation_bar">
                    <div class="buttn">
                        <a href="/back_toAppointments">Appointments</a>
                    </div>
                    <div class="buttn">
                        <a href="/Tpending">Pending</a>
                    </div>
                    <div class="buttn">
                        <a href="/logoutT">Logout</a>
                    </div>
                </div>
            
            <div class="pending_req book" style="color:black">
                <%if(locals.datas){%>
                <table >
                    <tr>
                        <th>appointment id</th>
                        <th>Student id</th>
                        <th>Date</th>
                        <th>Time</th>
                        <th>Reason</th>
                        <th>Action</th>
                    </tr>
                    <%for(var i =0;i<datas.length;i++){%>
                    <tr>
                        <td><%=datas[i].a_id%></td>
                        <td><%=datas[i].student_id%></td>
                        <td><%=datas[i].a_date%></td>
                        <td><%=datas[i].a_time%></td>
                        <td><%=datas[i].reason%></td>
                        <td>
                            <form action="/approve" method="post">
                                <input type="hidden" name ="appointment_id" value="<%=datas[i].a_id%>">
                                <button >approve</button>
                            </form>
                            <form action="/cancel" method="post">
                                <input type="hidden" name="appointment_id" value="<%=datas[i].a_id%>">
                                <button>cancel</button>
                            </form>
                        </td>
                    </tr>
                    <%}%>
                </table>
                <%}else{%>
                    <h4 style="color:green">No appointment request found</h4>
                <%}%>
            </div>


    </div>
</body>
</html>

